import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'work-calendar-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
